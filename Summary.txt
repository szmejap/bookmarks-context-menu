Creates selection menu items from bookmarks. Selected content is passed to the bookmarks URLs to enable search in any bookmarked webpage. Useful for custom search addresses, e.g. in search engines, dictionaries and any webpage with a search function.